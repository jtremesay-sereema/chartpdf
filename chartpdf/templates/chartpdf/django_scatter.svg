<svg class="cp-scatter" data-src="/api/samples.json?count=1" width="800" height="600" xmlns="http://www.w3.org/2000/svg" viewBox="0,0,800,600">
    <rect width="800" height="600" fill="magenta"></rect>
    {% for p in points %}
    <circle cx="{{ p.x }}" cy="{{ p.y }}" r="4" fill="green"></circle>
    {% endfor %}
</svg>